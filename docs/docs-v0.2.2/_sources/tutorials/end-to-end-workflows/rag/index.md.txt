# RAG

These tutorials build retrieval-augmented (RAG) applications that ground their answers in your own
documents instead of relying on a model's training data alone. Start here once you have a model
deployed and want a question-answering application in front of it.

```{toctree}
:maxdepth: 1

building-a-legal-contract-qa-assistant-on-dkubex
```
